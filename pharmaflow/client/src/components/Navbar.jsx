import React from 'react'; import {Bell,LogOut,Menu} from 'lucide-react'; import {useAuth} from '../hooks/useAuth'; import {useLocation} from 'react-router-dom'; export default function Navbar(){const {user,logout}=useAuth();const loc=useLocation();const title=loc.pathname.includes('medicine')?'Medicines':loc.pathname.includes('dispense')?'Dispensing':loc.pathname.includes('expiry')?'Expiry Alerts':loc.pathname.includes('history')?'Dispensing History':'Dashboard';return <header className="topbar"><div className="mobile-menu"><Menu size={20}/></div><div><div className="eyebrow">Pharmacy workspace</div><h1>{title}</h1></div><div className="top-actions"><Bell size={19}/><div className="avatar">{user?.name?.[0]?.toUpperCase()}</div><button className="icon-btn" onClick={logout} title="Logout"><LogOut size={18}/></button></div></header>}