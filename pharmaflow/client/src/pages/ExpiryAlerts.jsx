import React,{useEffect,useState}from'react';import{getExpiry}from'../api/dispensingApi';import{errorText}from'../utils/format';import SortDropdown from'../components/SortDropdown';import LoadingSpinner from'../components/LoadingSpinner';export default function ExpiryAlerts(){const[days,setDays]=useState('30'),[d,setD]=useState(null),[err,setErr]=useState('');useEffect(()=>{setD(null);getExpiry({days}).then(r=>setD(r.data.data)).catch(e=>setErr(errorText(e)))},[days]);return <><div className="page-head"><div><h2>Expiry alerts</h2><p className="muted">Prioritized batches that are close to or past expiry.</p></div><SortDropdown value={days} onChange={setDays} options={[{value:'7',label:'Next 7 days'},{value:'30',label:'Next 30 days'},{value:'90',label:'Next 90 days'}]}/></div><div className="card">{err&&<div className="error-box">{err}</div>}{!d?<LoadingSpinner/>:d.length?<div className="table-wrap"><table><thead><tr><th>Medicine</th><th>Batch</th><th>Quantity</th><th>Expiry</th><th>Days</th><th>Status</th></tr></thead><tbody>{d.map(x=><tr key={x._id}><td className="strong">{x.medicine?.name||x.medicineName}</td><td>{x.batchNumber}</td><td>{x.quantity}</td><td>{new Date(x.expiryDate).toLocaleDateString()}</td><td>{x.daysRemaining}</td><td><span className={`badge ${x.status}`}>{x.status}</span></td></tr>)}</tbody></table></div>:<div className="empty"><h3>No alerts</h3><p>No batches fall inside the selected expiry window.</p></div>}</div></>}
