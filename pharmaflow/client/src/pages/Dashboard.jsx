import React,{useEffect,useState}from'react';import{Package,Boxes,TriangleAlert,Trash2,Activity}from'lucide-react';import{getDashboard}from'../api/dashboardApi';import{getExpiry}from'../api/dispensingApi';import{errorText}from'../utils/format';import LoadingSpinner from'../components/LoadingSpinner';import ExpiryAlert from'../components/ExpiryAlert';export default function Dashboard(){const[d,setD]=useState(null),[alerts,setAlerts]=useState([]),[err,setErr]=useState('');useEffect(()=>{Promise.all([getDashboard(),getExpiry({days:30})]).then(([a,b])=>{setD(a.data.data);setAlerts(b.data.data||[])}).catch(e=>setErr(errorText(e)))},[]);if(err)return <div className="error-box">{err}</div>;if(!d)return <LoadingSpinner/>;return <><div className="page-head"><div><h2>Dashboard</h2><p className="muted">A live view of sellable stock and expiry risk.</p></div></div><div className="grid stats"><Stat I={Package} l="Total Medicines" v={d.totalMedicines}/><Stat I={Boxes} l="Sellable Stock" v={d.sellableStock}/><Stat I={TriangleAlert} l="Expiring ≤ 30d" v={d.expiringSoon}/><Stat I={Trash2} l="Expired Units" v={d.expiredStock}/><Stat I={Activity} l="Low Stock" v={d.lowStock}/></div><div className="grid two-col"><div className="card"><h3>Recent dispensing</h3>{d.recentDispensing?.length?<div className="table-wrap"><table><thead><tr><th>Medicine</th><th>Qty</th><th>Date</th></tr></thead><tbody>{d.recentDispensing.map(x=><tr key={x._id}><td className="strong">{x.medicine?.name||'Medicine'}</td><td>{x.quantity}</td><td>{new Date(x.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div>:<div className="empty">No dispensing history yet.</div>}</div><div className="card"><h3>Expiry watch · 30 days</h3>{alerts.length?alerts.slice(0,6).map(x=><ExpiryAlert key={x._id} batch={x}/>):<div className="empty">No upcoming expiry alerts.</div>}</div></div></>};function Stat({I,l,v}){return <div className="stat-card"><div className="stat-icon"><I size={19}/></div><div><div className="stat-label">{l}</div><div className="stat-value">{v}</div></div></div>}
