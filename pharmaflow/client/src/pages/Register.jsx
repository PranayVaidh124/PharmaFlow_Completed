import React,{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../hooks/useAuth';import{errorText}from'../utils/format';export default function Register(){const{register}=useAuth(),nav=useNavigate();const[f,setF]=useState({name:'',email:'',password:''}),[err,setErr]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setErr('');setBusy(true);try{await register(f);nav('/dashboard')}catch(x){setErr(errorText(x))}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-art"><Link className="brand" to="/"><span className="brand-mark">✚</span><span>Pharma<span>Flow</span></span></Link><h2>Start clean.</h2><p>Create a pharmacist workspace and begin tracking real batches with expiry-aware dispensing.</p></div><div className="auth-box"><form className="auth-card" onSubmit={submit}><h2>Create account</h2><p>Your first account is ready to manage inventory.</p>{err&&<div className="error-box">{err}</div>}<div className="field"><label>Name</label><input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></div><div className="field"><label>Email</label><input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></div><div className="field"><label>Password</label><input type="password" minLength="6" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></div><button className="btn primary" disabled={busy}>{busy?'Creating…':'Create account'}</button><p style={{marginTop:18,textAlign:'center'}}>Already registered? <Link className="link" to="/login">Sign in</Link></p></form></div></div>}
