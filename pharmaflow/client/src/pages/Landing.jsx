import React from 'react';import {Link} from 'react-router-dom';import {ShieldCheck,PackageCheck,Search,TriangleAlert,Layers,BarChart3} from 'lucide-react';const features=[['FEFO Dispensing','Automatically uses the earliest valid expiry batch first.',PackageCheck],['Sellable Stock','See true in-date inventory without counting expired units.',ShieldCheck],['Expiry Alerts','Spot critical, warning and upcoming batches at a glance.',TriangleAlert],['Medicine Search','Find medicines and batch information quickly.',Search],['Batch Tracking','Keep quantity and expiry details for every batch.',Layers],['Inventory Dashboard','Monitor stock, expiry exposure and recent activity.',BarChart3]];export default function Landing(){return <><section className="hero"><nav className="landing-nav"><Link className="brand" to="/"><span className="brand-mark">✚</span><span>Pharma<span>Flow</span></span></Link><div className="landing-links"><Link to="/login">Login</Link><Link className="btn primary" to="/register">Get Started</Link></div></nav><div className="hero-inner"><span className="pill">PHARMACY INVENTORY · FEFO</span><h2>Smart inventory management for <span>safer pharmacies.</span></h2><p>Track batches, protect against expired dispensing and let the backend automatically fulfill every request using First-Expiry-First-Out.</p><Link className="btn primary" to="/register">Start managing inventory →</Link></div></section><section className="landing-section"><div className="section-inner"><h3>Everything your pharmacy needs</h3><p>A focused inventory workspace built for independent, neighborhood, small-chain and clinic pharmacies.</p><div className="feature-grid">{features.map(([t,d,I])=><div className="feature" key={t}><I size={22} color="#1d8f68"/><h4>{t}</h4><p>{d}</p></div>)}</div></div></section><section className="landing-section alt"><div className="section-inner"><h3>How FEFO works</h3><p>PharmaFlow keeps the decision on the server so the inventory remains consistent.</p><div className="flow">{['Multiple Batches','Check Expiry','Remove Expired','Earliest Expiry','Dispense','Update Inventory'].map(x=><span key={x}>{x}</span>)}</div></div></section><section className="landing-section"><div className="section-inner"><h3>What comes next</h3><div className="feature-grid">{[['Supplier Management','Track suppliers, purchase orders and incoming stock.'],['Automated Notifications','Send alerts for upcoming expiries and low stock.'],['Sales & Inventory Analytics','Analyze dispensing patterns, turnover and expiry trends.']].map(([t,d])=><div className="feature" key={t}><h4>{t}</h4><p>{d}</p></div>)}</div></div></section></>}
