import React,{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../hooks/useAuth';import{errorText}from'../utils/format';export default function Login(){const{login}=useAuth(),nav=useNavigate();const[f,setF]=useState({email:'',password:''}),[err,setErr]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setErr('');setBusy(true);try{await login(f);nav('/dashboard')}catch(x){setErr(errorText(x))}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-art"><Link className="brand" to="/"><span className="brand-mark">✚</span><span>Pharma<span>Flow</span></span></Link><h2>Welcome back.</h2><p>Run your pharmacy inventory from one place, with FEFO enforcement at the API layer.</p></div><div className="auth-box"><form className="auth-card" onSubmit={submit}><h2>Sign in</h2><p>Access your pharmacy workspace.</p>{err&&<div className="error-box">{err}</div>}<div className="field"><label>Email</label><input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></div><div className="field"><label>Password</label><input type="password" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></div><button className="btn primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><p style={{marginTop:18,textAlign:'center'}}>No account? <Link className="link" to="/register">Create one</Link></p></form></div></div>}
