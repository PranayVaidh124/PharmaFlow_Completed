import Batch from'../models/Batch.js';import Medicine from'../models/Medicine.js';export function statusFor(date,now=new Date()){const d=(new Date(date)-now)/86400000;if(d<0)return'expired';if(d<=7)return'critical';if(d<=30)return'warning';if(d<=90)return'upcoming';return'safe'}export async function expiryAlerts(days=30){const now=new Date();const end=new Date(now.getTime()+Number(days)*86400000);const rows=await Batch.find({expiryDate:{$lte:end},quantity:{$gt:0}}).populate('medicineId','name').sort({expiryDate:1});return rows.map(b=>({ ...b.toObject(),medicine:b.medicineId,daysRemaining:Math.ceil((b.expiryDate-now)/86400000),status:statusFor(b.expiryDate,now)}))}