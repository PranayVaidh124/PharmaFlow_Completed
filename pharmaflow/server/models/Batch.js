import mongoose from'mongoose';const s=new mongoose.Schema({medicineId:{type:mongoose.Schema.Types.ObjectId,ref:'Medicine',required:true,index:true},batchNumber:{type:String,required:true,trim:true},quantity:{type:Number,required:true,min:0},receivedDate:{type:Date,required:true},expiryDate:{type:Date,required:true,index:true}},{timestamps:true});s.index({medicineId:1,expiryDate:1});s.index({medicineId:1,batchNumber:1},{unique:true});export default mongoose.model('Batch',s);