import test from'node:test';import assert from'node:assert/strict';function allocate(batches,qty,now=new Date('2026-09-17')){const valid=batches.filter(b=>b.quantity>0&&new Date(b.expiry)>now).sort((a,b)=>new Date(a.expiry)-new Date(b.expiry));const total=valid.reduce((s,b)=>s+b.quantity,0);if(total<qty)throw Error('Insufficient');let r=qty,out=[];for(const b of valid){if(!r)break;const take=Math.min(r,b.quantity);out.push([b.id,take]);r-=take}return out}test('FEFO skips expired and consumes earliest expiry first',()=>assert.deepEqual(allocate([{id:'A',quantity:30,expiry:'2026-09-20'},{id:'B',quantity:50,expiry:'2026-10-10'},{id:'X',quantity:100,expiry:'2026-09-10'}],70),[['A',30],['B',40]]));test('FEFO rejects insufficient sellable stock',()=>assert.throws(()=>allocate([{id:'A',quantity:50,expiry:'2026-09-20'}],70,/Insufficient/)));