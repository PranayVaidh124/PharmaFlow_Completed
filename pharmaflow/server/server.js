import'dotenv/config';import express from'express';import cors from'cors';import{connectDB}from'./config/db.js';import authRoutes from'./routes/authRoutes.js';import medicineRoutes from'./routes/medicineRoutes.js';import batchRoutes from'./routes/batchRoutes.js';import dispensingRoutes from'./routes/dispensingRoutes.js';import dashboardRoutes from'./routes/dashboardRoutes.js';import expiryRoutes from'./routes/expiryRoutes.js';import{notFound,errorHandler}from'./middleware/errorMiddleware.js';const app=express();app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173'}));app.use(express.json());app.get('/api/health',(_req,res)=>res.json({success:true,message:'PharmaFlow API is running'}));app.use('/api/auth',authRoutes);app.use('/api/medicines',medicineRoutes);app.use('/api/batches',batchRoutes);app.use('/api/dispense',dispensingRoutes);app.use('/api/dashboard',dashboardRoutes);app.use('/api/expiry-alerts',expiryRoutes);app.use(notFound);app.use(errorHandler);const port=process.env.PORT||5000;connectDB().then(()=>app.listen(port,'0.0.0.0',()=>console.log(`API listening on ${port}`))).catch(e=>{console.error('Database connection failed',e.message);process.exit(1)});