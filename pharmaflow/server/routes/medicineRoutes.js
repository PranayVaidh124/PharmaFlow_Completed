import{Router}from'express';import{list,getOne,create,update,remove,stock}from'../controllers/medicineController.js';import{list as batches,create as createBatch}from'../controllers/batchController.js';import{protect}from'../middleware/authMiddleware.js';const r=Router();r.use(protect);r.get('/',list);r.post('/',create);r.get('/:id/stock',stock);r.get('/:id/batches',batches);r.post('/:id/batches',createBatch);r.get('/:id',getOne);r.put('/:id',update);r.delete('/:id',remove);export default r;